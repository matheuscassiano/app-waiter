import styled from 'styled-components/native'

export const AddContainer = styled.TouchableOpacity`
    position: absolute;
    bottom: 16px;
    right: 16px; 
    elevation: 5;   
    border-radius: 50px;
`